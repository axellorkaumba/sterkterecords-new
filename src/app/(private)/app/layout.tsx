// Placeholder — coquille du dashboard artiste (nav, sélecteur d'artiste,
// notifications). Construite au Sprint 4 (§11.3 du CDC).
export default function ArtistDashboardLayout({ children }: { children: React.ReactNode }) {
  return <main className="flex-1 p-8">{children}</main>;
}
