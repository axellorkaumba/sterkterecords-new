/**
 * Placeholder — remplacé automatiquement par :
 *   pnpm supabase:gen:types
 * une fois le schéma Postgres créé (migrations dans supabase/migrations).
 * Ce fichier est dans .prettierignore/.eslintignore car généré : ne pas
 * l'éditer à la main une fois les vraies tables présentes.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
